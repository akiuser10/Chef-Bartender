# Example environment variables for deployment
# Railway will automatically set `DATABASE_URL` and `PORT` when you add a PostgreSQL plugin.

# Flask secret key (generate a secure random key for production)
SECRET_KEY=replace_me_with_a_secure_random_hex

# Database (Railway sets this automatically)
# DATABASE_URL=postgresql://user:pass@host:port/dbname

# Email / notification (optional)
# Use API-based email providers where possible (Resend, SendGrid)
RESEND_API_KEY=
SENDGRID_API_KEY=

# Optional SMTP settings (Railway may block SMTP ports; prefer API keys)
MAIL_SERVER=
MAIL_PORT=
MAIL_USE_SSL=true
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_DEFAULT_SENDER=

# Upload folder override (Railway persistent volume recommended at /data/uploads)
# If you add a volume, set this to /data/uploads or leave unset to auto-detect
UPLOAD_FOLDER=/data/uploads
